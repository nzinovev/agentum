# Go build output
/bin/
/dist/
*.exe
*.test
*.out

# sqlc-generated code is committed so a fresh clone builds without requiring
# sqlc to be installed. Regenerate after changing queries: `make sqlc-gen`.

# Dependencies
vendor/

# Local env / runtime
.env
.env.*
*.local

# Private design notes — not part of the public project. Kept locally for
# author/agent reference; must never be committed.
/reference/

# Editor / OS
.idea/
.vscode/
.DS_Store
